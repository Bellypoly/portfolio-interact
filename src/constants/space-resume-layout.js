/** Section count and indices for `space-resume` scroll-spy + `useScroll` targets (keep in sync with `SECTIONS` order). */
export const SECTION_COUNT = 7;
export const PREQUEL_SECTION_INDEX = 1;
export const WORK_SECTION_INDEX = 2;
export const EDUCATION_SECTION_INDEX = 4;
export const MISSION_GALLERY_GATE_SECTION_INDEX = 5;
export const PORTFOLIO_SECTION_INDEX = 6;

/** Marker click owns active index briefly so scroll-spy does not fight smooth-scroll. */
export const MARKER_JUMP_SUPPRESS_MS = 1200;
